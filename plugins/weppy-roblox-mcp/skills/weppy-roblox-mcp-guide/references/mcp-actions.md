# Dump legado de ações MCP

> Documento legado do WEPPY. O fluxo ativo do repositório está em `plugins/iluau/`.

O dump antigo foi removido para evitar manutenção de conteúdo obsoleto. Se você precisar do fluxo atual, use a documentação do iLuau.
